# Optional: use MongoDB. If unavailable, the backend automatically uses in-memory demo storage.
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB=mediintake

# Keep mock for demo. Implement provider logic in app/llm_client.py when ready.
LLM_PROVIDER=mock
